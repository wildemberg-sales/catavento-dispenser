// Telas de formulário/detalhe de item ficam com uma coluna só, então travar
// a largura numa tela grande (tablet) evita inputs/botões/imagens esticados
// de ponta a ponta — mesmo raciocínio usado no maxWidth do apps/desktop.
export const MAX_CONTENT_WIDTH = 480;
